# Session pause handoff — 2026-09-08 (morning resume notes)

Session ended mid-round on the user's request. Everything below is saved: items 1–2 are real
commits; items 3–4's code is in a labelled WIP commit on `main` (lint + tests + build all green
at pause time). This file is the resume instructions.

## Done and committed
- **Item 1 — wild night legibility**: commit `ac2539f`. NIGHT_LIFT 9 + NIGHT_HEMI 0.035, gated on
  `st.night`, zero by day. Verified: game close 21.5h ~7 (was 5.5), night waterhole improved, day
  unchanged. STATUS.json updated (environment round 4, game issue resolved).
- **Item 2 — kopje pride**: commit `2b75489`. Three-occluder root cause (skirt slope, lane-edge
  shrub, boulder at lens). Verified: male lion unmistakable at 8 m, deterministic camera. STATUS.json
  updated (savannah round 3, major resolved).

## Item 3+4 (terrain agent) — fixes APPLIED, verification PARTIAL → in WIP commit
- Files changed: `src/modules/terrain/{material.js, water.js, textures.js, README.md}`.
- **Item 3 verdict (real-GPU cross-check DONE)**: sparkle PERSISTS on real hardware — AMD RX 5700 XT
  via ANGLE D3D11. NOTE: default headless silently falls back to SwiftShader-Vulkan; must force
  `--use-angle=d3d11` for GPU checks. Evidence before: `tools/shots/gpu-check-game-overview-14.png`,
  `gpu-check-sav-overview-14.png` (+.json with gpu string). Genuine shader bug: wet-band roughness
  0.34 + mip-averaged detail normals = GGX speckle on banks/pan rims, day and night. Night "fence
  rings" are circular pan-rim wet bands, NOT zoning fences (no zoning edit made); residual night
  sparkle on road lines is roads' reflective paint (separate module, untouched).
- Fix applied: material.js wet roughness 0.34→0.50, +0.35 roughness floor 200–800 m, detail normal
  flattens to 15% past 700 m, odd-angle tile rotations (cache key v4); water.js glint ×0.1 by 800 m,
  wave normals flatten 200–800 m (cache key v5).
- **Item 4**: textures.js grass streak motif → locally-rotated domain-warped two-octave field
  (`bladesField`/`bladesMask`); dirt cracks → wobbled two-scale worley at half contrast
  (`crackField`). Palette untouched. One smoke shot verified (`after-terrain-close-16.png`: cracks
  gone, mottle softened, 0 errors).
- **RESUME STEPS (in order, one render page at a time)**:
  1. GPU after-shots via the D3D11 Playwright approach: game overview 14 + savannah overview 14 →
     READ vs `gpu-check-*-14.png` befores. Sparkle should be gone.
  2. Module shots: terrain overview/15, plains/16.5, escarpment/7, night/21.5, savannah overview →
     READ; check plains/escarpment palette unchanged vs `terrain-satcheck.png` reference look.
  3. Game shots (10–14 min each, budget ~1 h): overview 14, close 14 (mottle/cracks vs
     `blind-game-close-14.png`), overview 21.5, overview 8.
  4. If all good: orchestrator marks items 3+4 resolved in STATUS.json, commits as real commits.

## Items 5+6 (sim/park agent) — NOT STARTED (stopped during baseline, zero edits made)
- Next session: relaunch the sim/park builder with the original brief: (a) demo park net ≥ 0 $/day
  by day 30 at ≥1 price point via content that grows arrivals (species variety/attractions), not
  price tuning; (b) births = 0 diagnosis (rate vs happiness vs maturity gate), demonstrate births > 0
  in 30 days without ecology breaking; (c) harness scenarios for poaching, drought/disease, village
  prosperity. Existing 6 fidelity scenarios must stay semantically unchanged. 58/58 tests must hold
  (extend if sim logic changes). Owners: `src/modules/simulation/`, `src/modules/park/`;
  `tools/fidelity.mjs` additive scenarios only.

## Items 7, 8, final evaluation — PENDING (unchanged)
- 7: preset sweep with critic agents (environment dawn/dusk/storm/overcast/close; audio
  overview/storm/close; simulation boom/bust/close/night; zoning close/night; traffic night/sighting;
  tools terrain/road/close/night; ui report/panel/toolbar/night).
- 8: elephant skin bump, zoning boundary dashes, world-edge apron step, simulation `spend()` API
  (docs/requests/tools.md still unapplied).
- Final: re-run the blind visual + fidelity evaluation (round-3 protocol) and compare scores.

## Capture protocol reminders (learned this session, apply tomorrow)
- Vite HMR kills in-flight captures: batch edits → save → THEN capture; never edit while a shot
  runs. A killed shot's PNG is garbage (tiny, near-uniform) and its JSON has `fatal` + null stats.
- Real-GPU checks REQUIRE `--use-angle=d3d11` (default headless = SwiftShader-Vulkan silently).
- One render page at a time (two halve each other; 3+ starve). After any interrupted sweep:
  `taskkill //F //IM chrome-headless-shell.exe`.
- Game captures: 580–860 s wall each; module shots 25–120 s.
